<div class="row-fluid" style="padding: 10px 0px;" > 
  <div class='span8' > 
  	<div id='system_loading'>
  		<%= image_tag 'loading.gif', :alt => 'System loading', :title => 'System loading' %> Waiting for server response
    </div>
  </div>
  <div class='span4 right' >	
  <% if user_signed_in? %>
     Welcome,  <i class='icon-user'> <%= current_user.display_name %> </i>
     <%= link_button_signout  "Sign out", destroy_user_session_path, :method => :delete, :class => 'btn btn-mini btn-danger' %>
  <% else %>
     <%= link_to "Sign in", new_user_session_path %>
  <% end %>  
  </div>
</div>