<% content_for :breadcrumb do %>
  <%= breadcrumb [{ 'Categories' => admin_categories_path }, { 'Edit: ' + @category.name => nil }]   %>
<% end %>

<%= page_header("Edit Category : #{@category.name}") %>

<%= render :partial => 'form' %>
