<%= simple_form_for [:admin, @category ] , :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= f.input :description, :input_html => { :rows => 3} %>
  <div class="form-actions">
  	<%= button_save ' Save' %>
  	<%= link_button_cancel 'Cancel',admin_categories_path %>    
  </div>
<% end %>
